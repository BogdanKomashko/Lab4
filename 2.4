from sklearn import datasets
from sklearn.model_selection import train_test_split

# Завантаження даних
diabetes = datasets.load_diabetes()
X_d = diabetes.data
y_d = diabetes.target

# Розбиття на вибірки
X_train_d, X_test_d, y_train_d, y_test_d = train_test_split(X_d, y_d, test_size=0.5, random_state=0)

# Навчання
regr_d = linear_model.LinearRegression()
regr_d.fit(X_train_d, y_train_d)

# Прогноз
y_pred_d = regr_d.predict(X_test_d)

# Виведення коефіцієнтів та метрик
print("Coefficients:", regr_d.coef_)
print("Intercept:", regr_d.intercept_)
print("R2 score =", round(sm.r2_score(y_test_d, y_pred_d), 2))
print("MAE =", round(sm.mean_absolute_error(y_test_d, y_pred_d), 2))
print("MSE =", round(sm.mean_squared_error(y_test_d, y_pred_d), 2))

# Побудова графіка
fig, ax = plt.subplots()
ax.scatter(y_test_d, y_pred_d, edgecolors=(0, 0, 0))
ax.plot([y_d.min(), y_d.max()], [y_d.min(), y_d.max()], 'k--', lw=4)
ax.set_xlabel('Виміряно')
ax.set_ylabel('Передбачено')
plt.title('Завдання 2.4: Diabetes Dataset')
plt.show()
